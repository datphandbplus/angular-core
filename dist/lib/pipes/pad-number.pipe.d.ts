import { PipeTransform } from '@angular/core';
export declare class PadNumberPipe implements PipeTransform {
    /**
    * Transform
    * @param {any} input
    * @param {boolean} size
    * @return {string}
    */
    transform(input: any, size: number): string;
}
